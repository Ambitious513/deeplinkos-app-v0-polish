import { PageFrame } from "@/components/dashboard/page-frame";

export default function ProfilePage() {
  return (
    <PageFrame
      eyebrow="Settings"
      title="Profile"
      description="Identity, avatar, notifications, and session controls belong here."
    >
      <div className="panel">Profile settings placeholder.</div>
    </PageFrame>
  );
}

